import { Address } from "@project-serum/anchor";
import Decimal from "decimal.js";
import { OrcaDAL } from "../../dal/orca-dal";
export declare type LiquidityDataPoint = {
    liquidity: Decimal;
    price: Decimal;
    tickIndex: number;
};
export declare type LiquidityDistribution = {
    currentPrice: Decimal;
    currentTickIndex: number;
    datapoints: LiquidityDataPoint[];
};
export declare function getLiquidityDistribution(dal: OrcaDAL, poolAddress: Address, tickLower: number, tickUpper: number, refresh: boolean): Promise<LiquidityDistribution>;
