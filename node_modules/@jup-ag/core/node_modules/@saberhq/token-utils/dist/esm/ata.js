import { PublicKey } from "@saberhq/solana-contrib";
import { ASSOCIATED_TOKEN_PROGRAM_ID, TOKEN_PROGRAM_ID, } from "@solana/spl-token";
/**
 * Gets an associated token account address.
 */
export const getATAAddress = async ({ mint, owner, }) => {
    const [address] = await PublicKey.findProgramAddress([owner.toBuffer(), TOKEN_PROGRAM_ID.toBuffer(), mint.toBuffer()], ASSOCIATED_TOKEN_PROGRAM_ID);
    return address;
};
/**
 * Gets multiple associated token account addresses.
 */
export const getATAAddresses = async ({ mints, owner, }) => {
    const result = await Promise.all(Object.entries(mints).map(async (args) => {
        const [name, mint] = args;
        const result = await getATAAddress({
            mint,
            owner: owner,
        });
        return {
            address: result,
            name,
            mint,
        };
    }));
    const deduped = result.reduce((acc, { address, name, mint }) => {
        return {
            accounts: {
                ...acc.accounts,
                [name]: { address, mint },
            },
        };
    }, { accounts: {} });
    return {
        accounts: deduped.accounts,
    };
};
//# sourceMappingURL=ata.js.map