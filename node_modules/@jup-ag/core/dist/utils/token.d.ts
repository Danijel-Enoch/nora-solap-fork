import { Connection, PublicKey } from '@solana/web3.js';
import { Instruction } from './instruction';
export declare function createAndCloseWSOLAccount(connection: Connection, owner: PublicKey, amount: number): Promise<Instruction & {
    address: PublicKey;
}>;
export declare function findOrCreateAssociatedAccountByMint(connection: Connection, payer: PublicKey, owner: PublicKey, mintAddress: PublicKey | string, unwrapSOL: boolean): Promise<Instruction & {
    address: PublicKey;
}>;
